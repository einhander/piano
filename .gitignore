# Android
*.apk
*.apkm
*.aab
*.dex
*.class

# Gradle
.gradle/
build/
app/build/
.gradle/

# Android Studio
.idea/
*.iml
*.DS_Store
*.log

# Android SDK
# Android SDK
LOCAL_SDK

# CMake
!app/src/main/cpp/cmake-build-*
cmake-build-*/
*.ninja
*.cmake.cache

# Native libs
app/src/main/cpp/build/
app/src/main/cpp/3rdparty/build/

# FluidSynth build artifacts
app/src/main/cpp/3rdparty/fluidsynth/build/
app/src/main/cpp/3rdparty/fluidsynth/CMakeFiles/
app/src/main/cpp/3rdparty/fluidsynth/libfluidsynth.pc
app/src/main/cpp/3rdparty/fluidsynth/include/fluidsynth/

# Oboe build artifacts

# LSP Plugins integration (Phase 1). third_party/lsp is a git submodule
# pinned to upstream tag 1.2.34 (lsp-plugins/lsp-plugins). CI builds the
# LADSPA bundle from it via build-lsp-ladspa-android.sh and copies the .so
# into prebuilt/ before assembling the APK. The .so itself stays gitignored.
# The nested module repos under modules/ are fetched by `make fetch` at build
# time (not part of the submodule tree).
app/src/main/cpp/lsp-integration/prebuilt/**/*.so

app/src/main/cpp/3rdparty/oboe/build/

# Project files
*.live/
MyLiveSet/

# Build cache
app/build-cache/

# Local config
local.properties
app/local.properties

# Android signing
*.jks
*.keystore
*.keystore.bak

# CI/CD
!build/
build/*.apk

# Deepwork state (git-local, OpenCode-readable)
.slim/deepwork/

# OS
.DS_Store
Thumbs.db

# Build artifacts (explicit; the `!build/` CI negation above un-ignores app/build/)
app/build/
app/.cxx/

# AI tools
.opencode/
.claude/
.cursor/
.aider/
.codex/
.gemini/
.windsurf/
.roo/
.continue/